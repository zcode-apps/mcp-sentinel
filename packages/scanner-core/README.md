# MCP Sentinel Scanner Core Prototype

This package contains experimental detector code that is not currently used by the published CLI.

Important:

- Several detectors here were scaffolds or idea spikes and should not be treated as production-grade findings.
- The root package is the authoritative implementation for the npm release.
- This package is intentionally kept private until the detector logic is rebuilt around real fixtures and verified behavior.
